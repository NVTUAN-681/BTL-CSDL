git init
git remote add origin <url> : kết nối với git


4 câu lệnh đẩy:
git add <file muốn đẩy lên> / git add . (đẩy hết lên)
git commit -m '..': lưu tên lịch sử (.. = tên)
git push origin Tên_Nhánh


----------quy trình lần đăng nhập đầu tiên
mở git bash
gõ lệnh
git init
git remote add origin ...
git fetch origin
git pull origin/main


--quy trình sau lần đăng nhập đầu tiên
mở git bash
gõ lệnh
git pull
- chỉnh sửa file như thường
git add .
git commit -m'...'
git push origin main

 